---
- name: Install commons dependencies
  apt:  
    name:
      - tree
      - curl
      - htop
      - wget
      - vim
      - git
      - zip
      - unzip
    state: present
    update_cache: yes
